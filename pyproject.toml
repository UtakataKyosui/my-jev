[project]
name = "my-jev"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "UtaktaKyosui", email = "fill.ayaextech@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "mojo>=1.1.0",
    "typesafe-sdk>=0.7.0",
    "fastapi>=0.115",
    "uvicorn>=0.30",
    "httpx>=0.27",
]

[project.scripts]
my-jev = "my_jev:main"

[build-system]
requires = ["uv_build>=0.12.13,<0.13.0"]
build-backend = "uv_build"
